# Mi Proyecto

## Stack tecnológico
- Frontend: React + Vite (o lo que uses)
- Backend: Node/Express (o lo que uses)
- Base de datos: PostgreSQL (o lo que uses)

## Estructura del proyecto
- /frontend → aplicación cliente
- /backend → API REST
- /database → migraciones y seeds

## Convenciones de código
- Nombrar componentes en PascalCase
- Funciones en camelCase
- Usar async/await, evitar .then()

## Comandos útiles
- `npm run dev` → arrancar en desarrollo
- `npm run build` → build de producción
- `npm test` → tests

## Contexto importante
(Aquí describes qué hace tu app, qué problema resuelve)